

The user has waited 5 minutes for a QR from their Render bridge and got nothing. Need to diagnose where the chain is breaking. Let me check the Render service directly and the edge function.

Checking the bridge directly first.
