

# Fix Header Company Name in Print Badge

## Changes

### `src/pages/PrintBadge.tsx`

1. **Line 326** — Remove `font-style: italic;` (change to `font-style: normal;`)
2. **Line 527** — Change `Resustainability` to `Re Sustainability` (capital R, capital S, space between)
3. **Lines 640-641** — Also update the consent text occurrences of `Resustainability` to `Re Sustainability` for consistency

## Files Changed
- `src/pages/PrintBadge.tsx`

