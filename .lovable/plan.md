

# Rename "Submit Check-in" to "Submit" on Self-Service Page

## Change
In `src/pages/SelfService.tsx`, line 621, rename the button text from `'Submit Check-in'` to `'Submit'`.

## File Changed
- `src/pages/SelfService.tsx` — Update button label on Step 4

